# Node.add_child(node: Node, force_readable_name: bool = false)

**Parent**: Node

Adds a child node.

## Description

Adds a child node. Nodes can have any number of children, but every child must have a unique name.
